# Test Report 01 & 02 - Phase 1 & 2

## Group A — Old Behavior Tombstones
- Test 1: Phase 1 replaces `isRunning` with `isCampaignActive` and `start` with `enterCampaign`.
- Test 2: Phase 2 transitions from strict booleans to named `nodeState` enums.

## Group B — New Behavior Confirmation
- Test 1: `enterCampaign` initiates campaign mode correctly.
- Test 2: `reset` preserves `isCampaignActive`.
- Test 3: `exitCampaign` safely unbinds the state map.
- Test 4: `seenNodeIds` accumulates visited paths logically.
- Test 5: End nodes render as `complete` and disconnected nodes render as `failed`.

## Group C — Regression Suite
- Test 1: Store isolation protects the core narrative graph shape strictly during teardown (AR-08).

### Results:
- **8 passed, 0 failed**
- **REGRESSION: CLEAN**

Phase 1 and Phase 2 logic validated successfully.
