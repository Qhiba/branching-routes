# Fix Report 01 - Phase 1

### Fix 1
- **Issue addressed**: Self-Review Item 1 (Missing `// PRESERVED` comment for AR-08 on `exitCampaign`).
- **Code section**:
```javascript
  exitCampaign: () => {
    // PRESERVED: AR-08 (Simulation Isolation)
    set({
      isCampaignActive: false,
```
- **What was fixed**: Added the missing comment confirming that AR-08 isolation boundaries are strictly honored by the newly added exit campaign method.
- **Impact**: Affects the preservation list (adds documentation confirmation).

### Fix 2
- **Issue addressed**: Human Note 1 ("A bug, where it still possible to create a node on campaign mode.").
- **Code section**:
```javascript
  const lastClickTime = useRef(0);
  const onPaneClick = useCallback((event) => {
    if (isCampaignActive) return; // FIX: Prevent node creation in campaign mode
    clearSelection();
```
- **What was fixed**: Disallowed background pane clicks (used to add nodes) to register when `isCampaignActive` is true. Also added `isCampaignActive` to the `useCallback` dependency array to ensure the closure properly reads the latest simulation state.
- **Impact**: Affects the behavior delta (ensures authoring constraints mentioned in the intent actually fully apply to canvas interactions).

### Fix 3
- **Issue addressed**: Human Note 2 ("A bug, where it still possible to control / edit from the sidebar like `flag`, `status`, and `paths / chapter`.").
- **Code section**:
```javascript
  const selectedNodeId = useUIStore(state => state.selectedNodeId);
  const selectedEdgeId = useUIStore(state => state.selectedEdgeId);
  const isCampaignActive = useSimulationStore(state => state.isCampaignActive);

  return (
    // ...
      <div className="sidebar-content" style={{ flex: 1, overflowY: 'auto', padding: '16px', pointerEvents: isCampaignActive ? 'none' : 'auto', opacity: isCampaignActive ? 0.5 : 1 }}>
```
- **What was fixed**: Visually disabled and blocked clicks onto the sidebar settings content while a campaign is underway.
- **Impact**: Affects the behavior delta (guarantees authoring controls remain correctly disabled during gameplay).

## Files Modified
1. `f:\Projects\Web\branching-routes\src\store\simulationStore.js`
2. `f:\Projects\Web\branching-routes\src\components\GraphCanvas.jsx`
3. `f:\Projects\Web\branching-routes\src\components\Sidebar.jsx`
