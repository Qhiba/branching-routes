# Fix Report — 0306_fix_04.md

## Status
**Completed.** Addressed user feedback and missing tracking comments flagged during the Phase 4 self-review.

## Changes Made
1. **Option Hover Enhancement:**
   - Modified `.choice-node__option--clickable:hover:not(.choice-node__option--selected)` in `global.css` to remove the `:not(.choice-node__option--dimmed)` guard.
   - Added a specific rule for dimmed hovered options to gently raise opacity (`opacity: 0.7`) instead of fully overriding their dim state, granting clear interactive feedback without breaking the visual hierarchy.
2. **Immediate Badging on Exit:**
   - Included `isCampaignActive` in the dependency array of the passive analysis `useEffect` trigger within `GraphCanvas.jsx`. 
   - Exiting Campaign mode now instantly triggers structural reachability analysis, restoring warning badges without waiting for a manual topology edit.
3. **Esc Selection Clear:**
   - Bound a window-level `keydown` listener in `GraphCanvas.jsx` that captures the `Escape` key.
   - Triggers `clearSelection()` via `useUIStore`, deselecting the current node/edge and returning the sidebar to its neutral empty state.
4. **Missing Tracking Comments:**
   - Inserted missing `// ADDED: Phase 4` line comments into `CommonNode.jsx`, `ChoiceNode.jsx`, `EndingNode.jsx`, and `Sidebar.jsx`, ensuring full audibility against the original behavioral delta.

## Observations
All bug-fixes and quality-of-life requests merged cleanly without violating any AR constraints. The simulation workflow feels substantially smoother with the `Escape` shortcut and forgiving hover states.

Ready to proceed with final testing.
