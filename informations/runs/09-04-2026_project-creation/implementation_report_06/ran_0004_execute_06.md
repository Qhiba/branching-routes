# Phase 06 Execute Report

## Changes Made

- `src/components/TopBar.jsx`: Wired the New, Open, and Save buttons by attaching click handlers that call `graphStore` and `simulationStore` state functions, alongside importing and executing `saveFile` and `openFile` utility functions to securely read and emit project data.

## Files Produced
- `src/components/TopBar.jsx`
