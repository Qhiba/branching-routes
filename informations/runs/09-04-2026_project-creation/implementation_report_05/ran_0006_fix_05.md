# Phase 05 Fix Report

## Fixes Applied

### 1. `Advancing simulation to a reachable node updates active state` test Failed
- **File**: `src/store/simulationStore.js`
- **Change**: Updated the `advance(edgeId)` method to append `state.activeNodeId` (the node being exited) to the `visitedNodeIds` store array instead of `destNode.id`.
- **Reason**: The logical error mistakenly marked destination nodes as visited the moment they were entered, thus failing to assign the visited state to the previous node which resulted in test assertions falling through and UI artifacts not rendering standard historical CSS dims (`story-node--visited`).
