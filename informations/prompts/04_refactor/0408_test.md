## ROLE
You are writing behavioral parity tests for a structural refactor.
Your only question is: does the system behave exactly as it did before?
Not better. Not worse. Identical.

<!-- pipeline: 0405 Execute → 0406 Self-Review → 0407 Fix → 0408 Test -->

## CONTEXT
Current phase: [N] = 3

Load these files:
1. `/informations/runs/[DD-MM-YYYY]_refactor/ran_0404_phase_[N].md` — what this phase built
2. `/informations/runs/[DD-MM-YYYY]_refactor/ran_0402_first-audit.md` — behavioral invariants to verify
3. `/informations/runs/[DD-MM-YYYY]_refactor/ran_0404_invariants.md` — full invariant preservation plan
4. Load the most recent code for this phase:
   - If `ran_0407_fix_[N].md` exists → load that
   - If not → load `ran_0405_execute_[N].md`
5. All files listed under "Produces" in `ran_0404_phase_[N].md` — the actual current code to test

## TASK
Write a behavioral parity test suite covering only the invariants
relevant to this phase (listed in `ran_0404_phase_[N].md`):

### Group A — Invariant Verification
One test per behavioral invariant relevant to this phase.
Each test must:
- Be named exactly after the invariant it tests (e.g. `TEST_BI_04`)
- Prove the invariant holds in the refactored code
- Inline all functions — no imports from `src/`

### Group B — Data Contract Verification
For every data format, ID convention, or export format
listed in `ran_0402_first-audit.md §2` that this phase touches:
- Input the same data as before
- Assert the output is structurally identical

### Group C — Migration Verification
Only required if this phase has a migration step.
For each migration point:
- Prove old format still loads correctly
- Prove new format is produced correctly
- Prove both formats coexist without error

End with:
- X passed, Y failed
- PARITY: CONFIRMED / BROKEN
  (BROKEN if any Group A invariant test fails)

Produces:
- The complete standalone test file saved to:
  `tests/test_refactor_phase_[N].js`

After writing the file, give the user this instruction:
"Open your terminal and run: `[HOW TO RUN THE TEST]`
You will see PASS or FAIL for each test. Share the results back here."

## Save Report
Save your report to:
`/informations/runs/[DD-MM-YYYY]_refactor/implementation_report_[N]/ran_0408_test_[N].md`

## CONSTRAINT
- Test logic only — not UI rendering
- Inline all functions — do not import from src/
- Write plain standalone test code matching [STACK] — no test framework assumed
- These tests must be runnable on both old and new code —
  any test that only passes on the refactored code is a new behavior test,
  flag it as: NEW BEHAVIOR — not a parity test
- Do not run the test file yourself — output the file only
- Running the test is the human's job