## ROLE
You are fixing issues found in a structural refactor.
This is the most constrained fix context in the system.
You fix structural problems only. You do not change behavior.

<!-- pipeline: 0405 Execute → 0406 Self-Review → 0407 Fix → 0408 Test -->

## CONTEXT
Current phase: [N] = 2

Your notes (plain language, optional):
1. A warning appear on creating new graph `[Violation] 'click' handler took 1682ms`

Load these files:
1. `/informations/runs/[DD-MM-YYYY]_refactor/ran_0404_phase_[N].md` — what this phase planned to do
2. `/informations/runs/[DD-MM-YYYY]_refactor/ran_0402_first-audit.md` — behavioral invariants all fixes must preserve
3. `/informations/docs/architecture_rules.md` — rules all fixes must comply with
4. `/informations/runs/[DD-MM-YYYY]_refactor/implementation_report_[N]/ran_0406_self-review_[N].md` — review findings to fix
5. All files listed under "Produces" in `ran_0404_phase_[N].md` — the actual code to fix
6. `/informations/runs/[DD-MM-YYYY]_refactor/implementation_report_[N]/ran_0405_execute_[N].md` — what execute reported changing in this phase

## TASK
Fix every reported issue. For each fix:
- Issue number from the review
- The corrected code section only — not the whole file unless the whole file changed
- One sentence: what structural problem was fixed
- One sentence: confirm no behavior changed

If 0406 returned PASS and you have no notes: skip this prompt entirely.

## Save Report
Save your report to:
`/informations/runs/[DD-MM-YYYY]_refactor/implementation_report_[N]/ran_0407_fix_[N].md`

## CONSTRAINT
- Fix structural problems only
- If fixing an issue requires changing behavior, stop immediately:
  ESCALATE — [fix requires behavior change, cannot proceed]
- Do not touch code outside the reported issues
- Do not fix bugs noticed but not reported — comment them:
  // NOTE: unrelated issue — not touching in refactor
- All fixes must comply with architecture rules from `architecture_rules.md`