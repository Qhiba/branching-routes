## ROLE
You are fixing issues found in a new feature addition.
You fix only what was reported. You protect everything else.

<!-- pipeline: 0203 Execute → 0204 Self-Review → 0205 Fix → 0206 Test -->

## CONTEXT
Current phase: [N]

Your notes (plain language, optional):
<!-- WHAT YOU NOTICED — OR "none" -->
Current Implementation changes:
1. `creation bar` button for `common`, `choice`, and `ending` node should make the node appears at the middle of the current canvas viewport, while the other way of node creation still retain the current behaviour.

Unplaned Changes:
1. Creating the nodes should start from a `modal` first. The loop would be Create Node -> Node (`common`, `choice`, and `ending`) modal shows up -> Node Appears / rendered / created.
2. Pressing `ESC` should close the modal and cancel the node creation.
3. Deleting `flags` and `status` will show the guard, but the current one shows the id of the node. This is not user friendly. I think it should show the `node label` and there are a button to `focus` on the node.

If `ran_0204_self-review_[N].md` returned PASS and your notes
are "none": skip this prompt entirely.

Load these files:
1. `/informations/runs/[DD-MM-YYYY]_feature/ran_0202_phase_[N].md` — what this phase planned to add
2. `/informations/runs/[DD-MM-YYYY]_feature/ran_0202_featuredelta.md` — full feature delta
3. `/informations/runs/[DD-MM-YYYY]_feature/ran_0202_integrationpoints.md` — existing connections to protect
4. `/informations/docs/architecture_rules.md` — rules all fixes must comply with
5. `/informations/runs/[DD-MM-YYYY]_feature/implementation_report_[N]/ran_0204_self-review_[N].md` — review findings
6. `/informations/runs/[DD-MM-YYYY]_feature/implementation_report_[N]/ran_0203_execute_[N].md` — execute report for reference
7. All files listed under "Produces" in `ran_0202_phase_[N].md` — the actual code to fix

## TASK
Fix every reported issue from the review and every item raised
in the human notes. The AI translates human notes into
technical fixes — the user does not need to know how.

For each fix:
- Issue number or human note being addressed
- The corrected code section only — not the whole file
  unless the whole file changed
- One sentence: what was fixed
- One sentence: does this affect the feature delta,
  an integration point, or neither?

## Save Report
Save your report to:
`/informations/runs/[DD-MM-YYYY]_feature/implementation_report_[N]/ran_0205_fix_[N].md`

Report lists every file modified with its full path.

## CONSTRAINT
- Fix only reported issues and human notes
- Do not touch code outside the reported issues
- Do not fix the unrelated bug noted in ADDED/MODIFIED
  comments — that is for a separate push
- Do not add features beyond the current phase scope
- If a fix touches a file outside the phase file map,
  flag it before proceeding:
  SCOPE EXPANSION — [what would change and why]
  Do not proceed silently
- If fixing one issue creates an integration risk,
  say so explicitly
- All fixes must comply with architecture_rules.md