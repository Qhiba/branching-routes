## REFLECT — [DATE] — [PUSH NAME]

> **Input:** Review these before reflecting:
> - Your scope: `informations/runs/[DD-MM-YYYY]_project-creation/ran_0002_scope.md`
> - The audit verdict: `informations/runs/[DD-MM-YYYY]_project-creation/ran_0008_audit.md`
>
> **Output:** Copy this template into your project and fill it in.
> Save to: `informations/runs/[DD-MM-YYYY]_project-creation/ran_0011_reflect.md`

---

### What the plan said would happen
[ONE SENTENCE]
<!-- example: "Phase 1 would produce a working graph editor with node creation, connections, and JSON export." -->

### What actually happened
[ONE SENTENCE]
<!-- example: "All features shipped, but the export function needed two rounds of fixes due to missing metadata fields." -->

### What slowed me down
[ONE THING]
<!-- example: "The Plan didn't specify the exact JSON metadata format, so Execute had to guess and Audit caught the mismatch." -->

### What assumption was wrong
[ONE THING, OR "NONE"]
<!-- example: "Assumed React Flow's default edge type would work — had to switch to custom edges for label support." -->

### What changes in the next loop
- Scope: [any scope rule to add?]
  <!-- example: "Always define export format with an example JSON in the Plan step." -->
- Prompts: [any prompt to improve?]
  <!-- example: "Plan §4 needs to require a concrete example JSON, not just field names." -->
- Worker assignments: [any reassignment needed?]
  <!-- example: "Self-Review missed the metadata issue — consider a stronger model for that step." -->

### Carry forward to next push
[ONE SENTENCE — the seed for the next Brainstorm]
<!-- example: "Add browser-based save/load using IndexedDB so the user doesn't lose work on refresh." -->