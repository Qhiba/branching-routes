## ROLE
You are a QA engineer writing test cases for a new application.
You think in edge cases and failure modes.

<!-- pipeline: 0004 Execute → 0005 Self-Review → 0006 Fix → 0007 Test -->

## CONTEXT
Current phase: `[N] = [2]`

Load these files:
1. `ran_0003_phase_[N].md` — what this phase built
2. `ran_0003_datamodel.md` — validate outputs against this
3. Load the most recent code for this phase:
   - If `ran_0006_fix_[N].md` exists → load that
   - If not → load `ran_0004_execute_[N].md`
4. `ran_0002_scope.md` — to determine the correct test style for [STACK]
5. All files listed under "Produces" in `ran_0003_phase_[N].md` — the actual current code to test

## TASK
Write tests that cover:

1. **Happy path** — expected inputs produce expected outputs
2. **Edge cases** — empty arrays, null values, missing fields
3. **Failure cases** — what should break and how it should break
4. **Data integrity** — does the output match the data model from Plan §4?

Format:
- Each test must print PASS or FAIL with a description
- Group tests by function name
- End with a summary: X passed, Y failed

Produces:
- `tests/test_phase_[N].js` — the complete standalone test file

After writing the file, give the user this instruction:
"Open your terminal and run: `node tests/test_phase_[N].js`
 You will see PASS or FAIL for each test. Share the results back here."

**FOR USER** give the output of the test file.
**FOR AI** Update the report file after user output.


## Save Report
Save your report inside `/informations/runs/[DD-MM-YYYY]_project-creation/implementation_report_[N]/ran_0007_test_[N].md`

## CONSTRAINT
- Do not test UI rendering — test logic functions only
- If a test requires a fixture, define it inline at the top of the test file
- Write plain standalone test code that matches [STACK] — no test framework
- Validate outputs against the data model from `ran_0003_datamodel.md`, not arbitrary expectations
- Do NOT run the test file yourself — output the file only
- Running the test is the human's job