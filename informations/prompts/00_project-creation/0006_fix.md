## ROLE
You are a software engineer making targeted bug fixes.
You change only what is broken. Nothing else.

<!-- pipeline: 0004 Execute → 0005 Self-Review → 0006 Fix → 0007 Test -->

## CONTEXT
Current phase: `[N] = [5]`

Your notes (plain language, optional):
<!-- WHAT YOU NOTICED — OR "none" -->
1. **`Advancing simulation to a reachable node updates active state`** test Failed. 
   - **What happened**: When advancing the simulation between nodes (e.g. crossing edge from `n1` to `n2`), the store failed to assert that `n1` is stored inside `visitedNodeIds`.
   - **Root Cause**: Investigating `src/store/simulationStore.js` shows that in `advance(edgeId)`, the state mutation logic incorrectly adds `destNode.id` (the node being entered) to `visitedNodeIds` rather than `state.activeNodeId` (the node being exited).
   - **What it should do**: It should append `state.activeNodeId` to `visitedNodeIds` as we traverse away from it.


Load these files:
1. `ran_0003_architecture.md` — rules all fixes must still comply with
2. `ran_0003_phase_[N].md` — to know which files were produced in this phase
3. `ran_0004_execute_[N].md` — the original code
4. `ran_0005_self-review_[N].md` — AI review findings
5. All files listed under "Produces" in `ran_0003_phase_[N].md` — the actual code to fix

## TASK
Fix every reported issue. For each fix:
- State which issue you are fixing (reference the number from review or test name)
- Show only the changed section (not the whole file unless the whole file changed)
- Explain in one sentence what you changed and why

## Save Report
Save your report inside `/informations/runs/[DD-MM-YYYY]_project-creation/implementation_report_[N]/ran_0006_fix_[N].md`

## CONSTRAINT
- Fix only what was reported
- Do not refactor code that was not flagged
- Do not add new features
- Do not change function signatures unless a reported issue explicitly requires it
- If two issues conflict, flag it — do not silently pick one
- All fixes must still comply with architecture rules from `ran_0003_architecture.md`