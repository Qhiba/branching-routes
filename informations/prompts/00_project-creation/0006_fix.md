## ROLE
You are a software engineer making targeted bug fixes.
You change only what is broken. Nothing else.

<!-- pipeline: 0004 Execute → 0005 Self-Review → 0006 Fix → 0007 Test -->

## CONTEXT
Current phase: `[N] = [4]`

Your notes (plain language, optional):
<!-- WHAT YOU NOTICED — OR "none" -->
1. Does a warning count as something that need to fix? This is the warning from the devtool: "A form field element should have an id or name attribute. A form field element has neither an id nor a name attribute. This might prevent the browser from correctly autofilling the form. To fix this issue, add a unique id or name attribute to a form field. This is not strictly needed, but still recommended even if you have an autocomplete attribute on the same element. 1 resource: Violating node.
2. I need a `snapping toggle` to make the node snap to the grid. (possible `phase 3` problem)
3. Remove `React Flow logo` on the below-right-corner (possible `phase 2` problem)
4. field dropdown are on light mode, it hurt my eye.
5. A need for `AND of ORs` logic architecture for more complex narrative flow.
6. Seperation of Flags, Flag are for Boolean condition only, Status_Point are for numerical logic or Numbers.
7. Status_point default have a min_value of null (infinite) and max_value of null (infinite) and it can be edited on the right side bar.
8. The edge area of click need to be expand a little bit while keeping the thickness of the edge.
9. Can't check DevTools: `useGraphStore.getState().edges[0].sideEffects`, maybe I code it wrong?
10. Seperation of Inspector and Flag Side Bar. Making it a flyover(?) modal are better I think, you can argue with me on this.
11. Check the initial data model, do all node are implemented? is it on another phases? Is there any Variable that are missing on the UI?
12. The dropdown selection still on light mode, or should I say a bright white color.
13. A Bug where creating edge on already connected node (A to B) over and over again is possible.
14. The auto focus(?) when creating a node is bothering me, is it because double clicking base action are to `focus` on clicked location? (possible `phase 3` problem).


Load these files:
1. `ran_0003_architecture.md` — rules all fixes must still comply with
2. `ran_0003_phase_[N].md` — to know which files were produced in this phase
3. `ran_0004_execute_[N].md` — the original code
4. `ran_0005_self-review_[N].md` — AI review findings
5. All files listed under "Produces" in `ran_0003_phase_[N].md` — the actual code to fix

## TASK
Fix every reported issue. For each fix:
- State which issue you are fixing (reference the number from review or test name)
- Show only the changed section (not the whole file unless the whole file changed)
- Explain in one sentence what you changed and why

## Save Report
Save your report inside `/informations/runs/[DD-MM-YYYY]_project-creation/implementation_report_[N]/ran_0006_fix_[N].md`

## CONSTRAINT
- Fix only what was reported
- Do not refactor code that was not flagged
- Do not add new features
- Do not change function signatures unless a reported issue explicitly requires it
- If two issues conflict, flag it — do not silently pick one
- All fixes must still comply with architecture rules from `ran_0003_architecture.md`