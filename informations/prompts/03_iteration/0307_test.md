<!-- 0307_test.md -->

## ROLE
You are a QA engineer testing a behavioral change.
You must prove three things: old behavior is gone where intended,
new behavior works correctly, and unchanged behavior is unaffected.

<!-- pipeline: 0304 Execute → 0305 Self-Review → 0306 Fix → 0307 Test -->

## CONTEXT
Current phase: [N] = 2

Load these files:
1. `/informations/runs/[DD-MM-YYYY]_iteration/ran_0303_phase_[N].md` — what this phase built
2. `/informations/runs/[DD-MM-YYYY]_iteration/ran_0303_behaviordelta.md` — what changed and what stayed the same
3. `/informations/runs/[DD-MM-YYYY]_iteration/ran_0303_preservation.md` — preserved behaviors to verify
4. Load the most recent code for this phase:
   - Always load `ran_0304_execute_[N].md` — full picture of what the phase built
   - If `ran_0306_fix_[N].md` exists → load that too — corrections applied on top of execute
   - The fix report takes precedence where they conflict
5. All files listed under "Produces" in `ran_0303_phase_[N].md` — the actual current code to test

## TASK
Write three groups of tests:

### Group A — Old Behavior Tombstone
Tests that confirm the OLD behavior no longer exists.
These must FAIL on old code, PASS on new code.
One test per changed behavior in this phase.
Do not write a test that passes on both — that proves nothing.

### Group B — New Behavior Confirmation
Tests that confirm the NEW behavior works correctly.
Cover: happy path, edge cases, boundary values.

### Group C — Regression Suite
Tests for every item marked PROTECTED in
`ran_0303_preservation.md` that this phase touches.
These must PASS on both old and new code.
If behavior was preserved, prove it was preserved.

End with:
- X passed, Y failed
- REGRESSION: CLEAN / BROKEN
  (BROKEN if any Group C test fails)

Produces:
- The complete standalone test file saved to:
  `tests/test_iteration_phase_[N].js`

After writing the file, give the user this instruction:
"Open your terminal and run: [HOW TO RUN THE TEST]
You will see PASS or FAIL for each test.
Share the results back here."

After running the tests:
- If any test fails → open 0306 Fix, add the failure
  as a plain language note, then re-run 0307
- If all tests pass → tell **user** move to the next phase,
  or to 0308 Audit if all phases are complete

## Save Report
Save your report to:
`/informations/runs/[DD-MM-YYYY]_iteration/implementation_report_[N]/ran_0307_test_[N].md`

## CONSTRAINT
- Test logic only — not UI rendering
- Inline all functions — do not import from src/
- Write plain standalone test code matching the stack in
  `/informations/docs/project_overview.md` — no test framework assumed
- Group A tests must genuinely fail on old code —
  do not write tests that pass on both versions
- If a phase has no logic functions to test, state this explicitly
  and skip producing a test file rather than producing a
  placeholder that always passes
- Do not run the test file yourself — output the file only
- Running the test is the human's job