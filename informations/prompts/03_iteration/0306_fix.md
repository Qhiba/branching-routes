<!-- 0306_fix.md -->

## ROLE
You are fixing issues found in a behavioral change.
You are especially careful here — this is existing behavior.
Wrong fixes break things that were working before.

<!-- pipeline: 0304 Execute → 0305 Self-Review → 0306 Fix → 0307 Test -->

## CONTEXT
Current phase: [N]

Your notes (plain language, optional):
None

Load these files:
1. `/informations/runs/[DD-MM-YYYY]_iteration/ran_0303_phase_[N].md` — what this phase planned to do
2. `/informations/runs/[DD-MM-YYYY]_iteration/ran_0303_behaviordelta.md` — full behavior delta
3. `/informations/runs/[DD-MM-YYYY]_iteration/ran_0303_preservation.md` — what must survive unchanged
4. `/informations/docs/architecture_rules.md` — rules all fixes must comply with
5. `/informations/runs/[DD-MM-YYYY]_iteration/implementation_report_[N]/ran_0305_self-review_[N].md` — review findings and human notes
6. `/informations/runs/[DD-MM-YYYY]_iteration/implementation_report_[N]/ran_0304_execute_[N].md` — execute report for reference
7. All files listed under "Produces" in `ran_0303_phase_[N].md` — the actual code to fix

## TASK
Fix every reported issue from the review and every item raised
in the human spot-check notes. The AI translates human notes
into technical fixes — the user does not need to know how.

For each fix:
- Issue number or human note being addressed
- The corrected code section only — not the whole file
  unless the whole file changed
- One sentence: what was fixed
- One sentence: does this affect the behavior delta,
  the preservation list, or neither?

## Save Report
Save your report to:
`/informations/runs/[DD-MM-YYYY]_iteration/implementation_report_[N]/ran_0306_fix_[N].md`

Report lists every file modified with its full path.

## CONSTRAINT
- Fix only reported issues and human notes
- Do not touch code outside the reported issues
- Do not fix the unrelated bug noted in CHANGED comments —
  that is for a separate push
- If a fix shifts behavior outside the planned delta,
  flag it before proceeding:
  SCOPE EXPANSION — [what would change and why]
  Do not proceed silently
- If fixing one issue creates a regression risk, say so explicitly
- All fixes must comply with architecture_rules.md